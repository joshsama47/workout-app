import 'package:flutter/material.dart';

class WorkoutWeekView extends StatelessWidget {
  const WorkoutWeekView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Workout Week View'));
  }
}
