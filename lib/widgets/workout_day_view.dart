import 'package:flutter/material.dart';

class WorkoutDayView extends StatelessWidget {
  const WorkoutDayView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Workout Day View'));
  }
}
