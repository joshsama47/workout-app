import 'package:flutter/material.dart';

class WorkoutMonthView extends StatelessWidget {
  const WorkoutMonthView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Workout Month View'));
  }
}
